# WeChat UI Helpers

该目录用于承载微信模块中的只读展示 helper。

当前已覆盖：

- 联系人列表展示派生
- 线程列表摘要派生
- 当前选中联系人/群聊派生
- 变化原因面板的展示拼装

当前不放：

- 消息主链
- 消息状态写回
- 记忆系统调用
- 平台能力
- 高风险流程装配

说明：微信模块当前更适合继续停留在“UI + 轻量 domain”阶段。后续若没有明确消息主链边界，不建议为了层数统一而强行进入 `app/wechat`。
