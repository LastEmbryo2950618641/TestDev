# Taobao UI Helpers

该目录用于承载淘宝桌面应用的只读展示 helper。

当前约束：

- 放筛选标签、搜索提示、商品摘要、详情展示、钱包面板等只读视图逻辑。
- 不放商品生成、购买、状态写回、AI 调用、库存变更。
- 旧入口仍保留在 `publish/taobao-actions.js`，这里优先作为真实实现落点。

后续如果继续拆分，优先按“展示 / 行为 / 生成”边界拆开，不要重新回流到根目录。
